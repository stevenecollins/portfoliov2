<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>{{ title }}</title>
		<link rel="stylesheet" href="{{ '/scss/styles.css' | url }}" />
	</head>
	<body>
		<div class="page">
			{% include 'header.njk' %}
			<main>
				{{ content | safe }}
			</main>
			{% include 'footer.njk' %} {% include 'svg.njk' %}
			<script src="{{ './js/index.js' | url }}"></script>
			{% if pageId == 'home' %}
			<script src="{{ './js/avatar.js' | url }}"></script>
			{% endif %}
		</div>
	</body>
</html>
